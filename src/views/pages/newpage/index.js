import React from 'react';
import PropTypes from 'prop-types';

NewPage.propTypes = {
    
};

function NewPage(props) {
    return (
        <div>
            Hello
        </div>
    );
}

export default NewPage;